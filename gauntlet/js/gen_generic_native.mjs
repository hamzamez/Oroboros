// Code generated by oroboros. DO NOT EDIT.

export function nativeSumOf(a) {
	let acc = 0;
	let i = 0;
	for (;;) {
		if ((i >= (a.length))) {
			return acc;
		}
		const u1 = (acc + (a[i]));
		const u2 = (i + 1);
		acc = u1;
		i = u2;
		continue;
	}
}

export function nativeWordTally(text) {
	const ws = ((text.match(/\S+/g) ?? []));
	const init = (Object.create(null));
	let acc = init;
	let i = 0;
	for (;;) {
		if ((i >= (ws.length))) {
			return acc;
		}
		const w = (ws[i]);
		acc[w] = (((acc[w]) ?? 0) + 1);
		i = (i + 1);
		continue;
	}
}

